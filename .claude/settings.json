{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); case \"$f\" in *.java) cd \"$CLAUDE_PROJECT_DIR\" && ./gradlew spotlessApply -q ;; esac; exit 0",
            "timeout": 180
          }
        ]
      }
    ]
  }
}
